---
noteId: 96
---

What are HTTP response codes?

---

HTTP response codes are three-digit status messages sent by a web server to tell the client whether a request succeeded, failed, or needs further action.