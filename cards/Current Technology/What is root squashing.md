---
noteId: 1772627309112
---

What is root squashing?

---

A security feature in NFS that maps root user requests to an unprivileged user such as `nfsnobody` to prevent root level access on the shared system.
